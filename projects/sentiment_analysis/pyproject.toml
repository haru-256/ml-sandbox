[project]
name = "transformer"
version = "0.1.0"
description = "Full-scratch Transformer implementation with PyTorch"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
  "lightning~=2.6.5",
  "torchmetrics~=1.9.0",
  "bertviz~=1.4.1",
  "transformers[torch,onnxruntime,optuna,sentencepiece,sklearn,vision]~=4.57.6",
  "datasets~=2.21.0",
  "spacy~=3.8.14",
  "numpy~=2.5.0",
  "polars~=1.42.1",
  "rich~=13.9.4",
  "tensorboard~=2.21.0",
  "torchinfo~=1.8.0",
]

[dependency-groups]
dev = ["notebook~=7.6.0", "ipywidgets>=8.1.8", "tqdm~=4.68.3"]
test = ["pytest~=9.1.1", "pytest-mock~=3.15.1", "deepdiff~=9.1.0"]
lint = ["ruff~=0.15.20", "mypy~=1.20.2"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
extend-select = ["E", "F", "UP", "B", "SIM", "I", "ARG", "RUF"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
mypy_path = "."
exclude = [".venv"]
disable_error_code = ["import-untyped", "assignment", "unused-ignore", "attr-defined"]

follow_imports = "silent"
warn_redundant_casts = true
warn_unused_ignores = true
disallow_any_generics = true
no_implicit_reexport = true
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = ["torch.*", "deepdiff.*", "datasets.*", ".*", "timm"]
ignore_missing_imports = true

[tool.pytest.ini_options]
markers = [
  "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]
